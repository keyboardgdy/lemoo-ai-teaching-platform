# Lemoo Cloud

This Python project owns every cloud-side process entrypoint and the single `uv.lock`. W2 contains only health endpoints, safe configuration and fail-closed process placeholders; no domain handler or persistence behavior is implemented yet.

Run from the repository root through `task` so local and CI commands remain identical.
