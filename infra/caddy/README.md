# Caddy boundary

Caddy is the approved edge proxy but is not started in W2. TLS routes are introduced with the application profile and development PKI evidence; localhost-only W2 health checks do not constitute the production routing contract.
