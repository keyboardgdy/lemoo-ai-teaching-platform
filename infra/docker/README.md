# Container images

Multi-stage, non-root application images are introduced and verified in W7c after locked application artifacts and image security checks exist. The W2 Compose file pins named infrastructure image versions and does not define a production deployment.
