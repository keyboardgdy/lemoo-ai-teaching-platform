# Protocol schema status

Status: `not_started`

MQTT, WebSocket and Job schemas are created and frozen in W5. No implementation may depend on an unstated protocol before G2-Device or its capability-specific gate passes.
